{
  "name": "quiz-app",
  "version": "1.0.0",
  "description": "Interactive statistics quiz app for students preparing for exams.",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "build": "node scripts/build.js",
    "preview": "node scripts/preview.js"
  }
}
